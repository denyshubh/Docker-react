sudo: required
# super user level permission

services:
  - docker

before_install:
  - docker build -t denyshubh/docker-react -f Dockerfile.dev .

script:
  - docker run denyshubh/docker-react yarn run test -- --coverage
  